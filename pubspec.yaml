name: simple_chat_ai
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  fluent_ui: ^4.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  http: ^1.2.2
  intl: ^0.20.1
  logging: ^1.3.0
  uuid: ^4.5.1
  win32: ^5.10.0
  window_manager: ^0.4.2

dependency_overrides:
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
